"""Extract and enrich metadata from raw intake"""
def run_extract():
    print("Extract: TODO - Use Claude to extract metadata from raw URLs")
    # Will use Claude to visit URLs and extract: title, description, publish date, artifact type
