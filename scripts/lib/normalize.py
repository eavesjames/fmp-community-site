"""Normalize and dedupe items"""
def run_normalize():
    print("Normalize: TODO - Dedupe and normalize items into items.json")
    # Will check for duplicates, normalize metadata, assign confidence scores
